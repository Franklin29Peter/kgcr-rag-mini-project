# KGCR-RAG Mini Project

This mini-project extracts entities and relations from the given case-study text and represents the extracted information in a simple knowledge graph format.

## Technologies

- Python
- spaCy
- JSON

## How to Run

Create and activate a Python virtual environment, then install the required packages:

```bash
pip install -r requirements.txt
python -m spacy download en_core_web_sm